# Task 5 fix round 1 — non-synthesizable Defender authority

Read Task 5's report and scoped review. Fix all four findings without changing the 11 public names, receipt schema, candidate docs, or Setup/portable ordering.

## Required RED -> GREEN

1. Remove `Adapters` from the public `Invoke-CcodReleaseDefenderCheck` interface and remove the public script-library bypass. Put adapter-taking logic behind a non-exported module core used only through module-scope tests. Production adapters must resolve the real Defender cmdlets and actual Zone stream; no public parameter/capability may replace Zone, Defender status, scan, detections, clock, hashes, or writer. Add a RED that enumerates public parameters/exports and attempts the old forged receipt path.
2. Move or share deep portable/Setup manifest validation with `ReleaseAssetContract.psm1`. `Test-CcodExactReleaseAssetSet` and promotion must reject malformed nested payload/provenance/inventory, wrong file lengths/hashes, ZIP entry/root-launcher mismatch, and Setup nested payload/provenance mismatch even when outer manifests and receipt hashes are self-consistent. Replace bogus baseline fixture hashes with real internally consistent bytes and add one mutation per nested binding.
3. Hold read-only, no-write/no-delete shared handles for candidate/checksum/manifest/Zone from initial proof through scan, receipt durable read-back, and return. Promotion must hold all 11 asset handles and both receipt handles across its complete decision, revalidate identity/hash and exact directory membership before return, and reject scan ABA, post-revalidation replacement, and promotion-time replacement. Use canonical final-path/file-ID/reparse/ADS/multilink checks where the existing native helpers support them.
4. Serialize one canonical receipt byte sequence, write create-only, flush, no-replace publish, reopen the final leaf, verify regular/nonreparse/default-stream identity, strict UTF-8/duplicate-free/exact 25 fields, bytes/hash and every field against the expected receipt, then return. A no-op, corrupting, wrong-target, stale-object or write-then-replace adapter must fail. Failed scan receipts require the same read-back before the stable scan error is returned.

Run ReleaseWorkflow, PortableRelease, InstallLifecycle, all changed parsers, `git diff --check`, and the complete persistence aggregate from frozen source. Commit implementation and evidence separately and request a scoped fix-round review. Do not run real Defender/build/network/install/reboot/release operations. Tasks 6–8 remain blocked.
